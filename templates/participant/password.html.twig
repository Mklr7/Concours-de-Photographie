{% extends 'base.html.twig' %}

{% block title %}
	Modifier mon Mot de passe
{% endblock %}

{% block body %}
	<section class="bg-white dark:bg-gray-900 mt-20">
		<div class="py-8 px-4 mx-auto max-w-screen-xl sm:py-16 lg:px-6">
			<div class="max-w-screen-md mb-8 lg:mb-16">
				<h2 class="mb-4 text-4xl tracking-tight font-extrabold text-gray-900 dark:text-white">
					Modifier mon Mot de passe
				</h2>
				<p class="text-gray-500 sm:text-xl dark:text-gray-400">Ici vous pouvez Modifier votre mot de passe
				</p>
				<a href="{{'/participant'}}">
					<p class="text-blue-500 sm:text-xl dark:text-gray-400">Retourner à mon Espace Participant</p>
				</a>

			</div>

			<div>
				{% if notification %}
					<div class="p-4 mb-4 text-sm text-blue-800 rounded-lg bg-blue-50 dark:bg-gray-800 dark:text-blue-400" role="alert">
						{{ notification }}
					</div>

				{% endif %}
				<div class="space-y-8 md:grid md:grid-cols-2 lg:grid-cols-3 md:gap-12 md:space-y-0">
					{{form(form)}}
				  </div>


					{% endblock %}
